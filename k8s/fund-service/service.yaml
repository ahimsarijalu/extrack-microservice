apiVersion: v1
kind: Service
metadata:
  name: fund-service
  namespace: extrack
  labels:
    app: fund-service
spec:
  selector:
    app: fund-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
