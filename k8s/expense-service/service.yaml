apiVersion: v1
kind: Service
metadata:
  name: expense-service
  namespace: extrack
  labels:
    app: expense-service
spec:
  selector:
    app: expense-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
